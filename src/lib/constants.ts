/** Weight for substitution bonus in scoring (0-1). A substitution counts as this fraction of having the real ingredient. */
export const SUBSTITUTION_WEIGHT = 0.6;

/** Maximum number of recipe results to return */
export const MAX_RESULTS = 20;

/** Minimum score threshold to show a recipe (0-100) */
export const MIN_SCORE_THRESHOLD = 10;

/** Debounce delay for autocomplete filtering (ms) */
export const AUTOCOMPLETE_DEBOUNCE_MS = 150;

/** Max autocomplete suggestions to show */
export const MAX_AUTOCOMPLETE_SUGGESTIONS = 8;
